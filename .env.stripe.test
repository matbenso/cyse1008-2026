# Stripe test-mode credentials
# Students: sign up at stripe.com, create a sandbox, and add your own keys to .env.development
# STRIPE_SECRET_KEY=sk_test_...
NEXT_PUBLIC_SITE_URL=http://localhost:3032
# Students: run `stripe listen --forward-to localhost:3032/api/stripe/webhook`
# and add the printed secret to your local .env.development as STRIPE_WEBHOOK_SECRET
